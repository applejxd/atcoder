#include <vector>

int main() {
  uint N = 0;
  std::cin >> N;
  if (N < 1 || N > 100) {
    throw std::runtime_error("Input error!");
  }

  std::vector<uint> d(N);
  for (uint idx = 0; idx < N; ++idx) {
    
  }
}