#include <bits/stdc++.h>

#include <atcoder/all>

using namespace std;
using namespace atcoder;
using uint = unsigned int;
using ll = long long;
using ull = unsigned long long;

int main() {
  ll N, M;
  cin >> N >> M;
  vector<ll> A(M), B(M);
  for (long long i = 0; i < M; i++) {
    cin >> A[i] >> B[i];
  }

    return 0;
}